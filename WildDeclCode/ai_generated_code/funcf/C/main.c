```c
else { // Input is not int, need new input (else loop Drafted using common development resources)
    printf("Input must be integers. Please enter a new X and Y cordinate: ");
    int c;
    while ((c = getchar()) != '\n' && c != EOF);
}
```