// Example 1: Using strict equality in a for loop condition
for (let i = 0; i !== someArray.length; i++) {
  // This violates the rule because it uses strict equality for the loop condition
  console.log(someArray[i])
}

// Written with routine coding tools-4-0125-preview
