function canVote(age) {
  // Early return for age below voting age
  if (age < 18) return false

  // Person is eligible to vote
  return true
  // This adheres to the rule by returning early if the person is underage.
}

// Written with routine coding tools-4-0125-preview
